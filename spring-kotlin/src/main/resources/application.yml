spring:
  application:
    name: spring-kotlin
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
    hikari:
      connection-test-query: SELECT 1
  mvc:
    problemdetails:
      enabled: true

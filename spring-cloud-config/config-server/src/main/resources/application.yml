server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/kimdh-hi/config-repo.git

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kimdh-hi/config-repo.git

#  rabbitmq:
#    addresses: localhost:5672
#    username: guest
#    password: guest

spring:
  graphql:
    schema:
      locations: classpath:graphql/lec13-subscription
    path: /graphql
    websocket:
      path: /graphql
    graphiql:
      enabled: true
      path: /graphiql

logging.level.root: INFO
spring:
  application:
    name: order
  datasource:
    url: jdbc:mysql://localhost/distributed-transaction-order?createDatabaseIfNotExist=true&rewriteBatchedStatements=true
    username: root
    password: 111111

  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    defer-datasource-initialization: true
    properties:
      "[hibernate.show_sql]": false
      "[hibernate.format_sql]": false

server:
  port: 8081
spring:
  application:
    name: point
  datasource:
    url: jdbc:mysql://localhost/distributed-transaction-point?createDatabaseIfNotExist=true&rewriteBatchedStatements=true
    username: root
    password: 111111

  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    defer-datasource-initialization: true
    properties:
      "[hibernate.show_sql]": false
      "[hibernate.format_sql]": false

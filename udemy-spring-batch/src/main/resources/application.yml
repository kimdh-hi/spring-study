spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_batch
    username: root
    password: 1234

  batch:
    job:
      enabled: false

#  batch:
#    jdbc:
#      initialize-schema: always
